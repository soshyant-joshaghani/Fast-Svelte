{
  "name": "fast-svelte",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "check": "npm run check -w frontend",
    "test": "vitest run --config tests/frontend/vitest.config.ts",
    "test:watch": "vitest --config tests/frontend/vitest.config.ts"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
