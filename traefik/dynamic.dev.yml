# Dev routes — host processes (Vite + uvicorn) and TLS for *.localhost.

http:
  routers:
    fast-svelte-backend-host:
      rule: Host(`api.localhost`)
      entryPoints:
        - http
        - https
      service: fast-svelte-backend-host
    fast-svelte-frontend-host:
      rule: Host(`dashboard.localhost`)
      entryPoints:
        - http
        - https
      service: fast-svelte-frontend-host

  services:
    fast-svelte-backend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:8000
    fast-svelte-frontend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:5000

tls:
  stores:
    default:
      defaultGeneratedCert:
        resolver: default
        domain:
          main: localhost
          sans:
            - "*.localhost"
